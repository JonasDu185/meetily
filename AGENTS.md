# 项目协作规则

## 沟通与提交

- 使用中文交流和编写代码注释。
- commit message 使用中文。
- 未经用户确认，不提交、不推送、不创建远程仓库。
- 完成一个功能或连续修改几轮后，提醒用户运行 `/simplify`。

## 开始工作前

- 涉及源码、上游同步、依赖、构建或发布时，先读取 `.agents/skills/maintain-meetily-fork/SKILL.md`。
- 同时读取 `docs/UPSTREAM.md`、`docs/LEGAL.md`、`docs/ACCEPTANCE.md` 和 `docs/BUILD_BASELINE.md`。
- 先检查当前分支和工作区，保留用户已有改动。
- 不在 `main` 上直接开发。

## 产品范围

- 目标平台：用户本人的 Apple Silicon Mac。
- 基础：Meetily Community Edition v0.4.0。
- 保留内置本地 Whisper/Parakeet 模型管理，不接入付费转写 API。
- 当前 MVP：录音、来源分轨、混合录音、导出、本地转写、Markdown/PDF、中文搜索和会议提醒。
- 会议提醒必须由用户点击确认后才能开始录音。
- 会议摘要、大模型接入、具体姓名声纹识别和大规模界面重做暂缓。

## 合规与隐私

- 保留 MIT `LICENSE.md` 和原版权声明。
- 不复制、逆向或借用 Meetily Pro 闭源代码及商业资产。
- 同类 Pro 功能只能依据公开 API 和公开产品行为独立实现。
- 个人版默认关闭 PostHog 遥测和 Meetily 官方自动更新。
- 不把录音、转写、配置、密钥或日志发送到未明确批准的外部服务。

## 工程边界

- 当前支持的应用位于 `frontend/`；`backend/` 是历史代码，默认不用于新开发。
- 优先小步修改，避免无关重构和整文件覆盖。
- 新依赖必须说明必要性、许可证、体积和离线影响。
- 未经确认，不安装项目依赖、不初始化子模块、不下载模型、不修改全局环境配置。
- 真实录音数据不得加入 Git。

## 验证

- 每项改动必须写清验收标准和回滚方式。
- 先执行针对性测试，再执行受影响模块测试。
- 音频功能必须进行真实设备录制验证；仅编译通过不算完成。
- 中文搜索需覆盖中文、中英混合、表情符号和长文本。
- 未运行的测试必须明确标注为“未验证”。
